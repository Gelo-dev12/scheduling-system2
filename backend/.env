# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb+srv://shiftsync2:SyZUws89xYUhWWEg@cluster0.l3cpfwo.mongodb.net/auth?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Email Configuration (if needed later)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
EMAIL_USER=shiftsync2@gmail.com
EMAIL_PASS=czfu qkfd rqyo hquw

Hourly Rate: ₱${employee.rate ?? employee.hourlyRate ?? 0}/hr

Weekly Pay: ₱${((employee.rate ?? employee.hourlyRate ?? 0) * (employee.hoursPerWeek ?? 40)).toFixed(2)}
